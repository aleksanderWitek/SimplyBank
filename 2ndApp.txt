APLIKACJA BANKKOWA

Actor
Split to Client and ClientDetails?
Create Parent class User for Client and Admin or it's not necessary
- DTO Client:
	- Long id
	- String username
	- String password
	- String firstName
	- String lastName
	- String city
	- String street
	- String houseNumber
	- String identificationNumber // PESEL/PPS
	- UserType: Client
	- List<Account> accounts @ManyToMany

	* ClientService
		- findById(Long id)
		- findAll()
- DTO Admin
	- Long id
	- String adminNumber
	- String password
	- String firstName
	- String lastName
	- UserType: Admin

	* AdminService:
		- createAdmin(Admin admin) ???
		- removeAdmin(Long adminId)
		- findByIdAdmin(Long adminId)
		- findAll()
		- createClient(Client client)
		- removeClient(Long clientId)
		- createAccount(Account account, Long clientId)
		- removeAccount(Long accountId)
		- findAllTransactions()
		- approveCashDeposit(Long accountId, BigDecimal amount)
	
	* zakladac nowe konta
	* przegladac wszystkie transakcje
	* zatwierdzenie wplaty gotowkowej (to jest rodzaj tranzakcji ktory nie jest od
	nikogo i info kto to zatwierdzil)

- DTO Account:
	- Long id
	- String number
	- AccountType : current, saving, foreign currency
	- Currency: PLN, EUR
	- BigDecimal balance.
	- List<Client> clients @ManyToMany
	- List<Transaction> transactions @ManyToMany

	* AccountService
		- findById(Long id)
		- findAll()

	* Typ konta: Rozliczeniowe, oszczednosciowe, walutowe
	* Waluta PLN/EUR
	* Balance(stan konta)

- DTO Transaction:
	- Account accountFrom
	- Account accountTo
	- BigDecimal value
	- LocalDateTime date

	* TransactionService
		- findById(Long id)
		- findAllForSelectedAccount(Account account)
		- createTransaction(Account accountFrom, Account accountTo, BigDecimal Long)

- Widok transakcji
	* From To - dane osoby przelewajacej, date, zobaczyc jak to w apce mam i podobnie
	* Wyszukiwanie po dacie i po danych osoby

- Przelej (przelew: jedno konto na + a drugie konto na -) 
(czyli minus tyle do kogos a ta druga osoba plus tyle od kogos)
	* Account accountFrom
	* Account accountTo
	* BigDecimal value

- Dodatkowa opcja dla Adminow zeby zakladac konta po zalogowaniu

- Czyli tabelka dla user_account , back_account ManyToMany

- Logowanie: Login + Haslo

- Widok kont (klient moze miec 1 lub wiecej kont)

**** TESTY ****
- Integracyjne!!!
- junit - jednostkowe tez zrobic
